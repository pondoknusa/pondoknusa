@props(['name', 'value' => '', 'rows' => 4, 'placeholder' => null])

<textarea class="ui-textarea" id="{{ name }}" name="{{ name }}" rows="{{ rows }}" placeholder="{{ placeholder }}" {!! $attributes.toHtml() !!}>{{ value }}</textarea>